{
  "name": "ge-backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "repository": "https://github.com/GE-MDS-FNM-V2/backend.git",
  "author": " ",
  "license": "MIT",
  "devDependencies": {
    "electron": "^7.1.1",
    "electron-packager": "^14.1.0",
    "electron-rebuild": "^1.8.6",
    "shelljs": "^0.8.3"
  },
  "scripts": {
    "build": "yarn electron-rebuild && yarn build:clone-frontend && yarn electron-packager . --asar",
    "start": "yarn electron-rebuild && electron .",
    "build:clone-frontend": "node scripts/cloneFrontend.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "electron-is-dev": "^1.1.0",
    "express": "^4.17.1",
    "serialport": "^8.0.5"
  },
  "build": {
    "files": [
      "src/**/*",
      "node_modules/**/*"
    ]
  }
}
